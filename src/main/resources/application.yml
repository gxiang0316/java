server:
  port: 8081

#spring:
  #datasource:
   #   url: jdbc:mysql://localhost:3306/activiti?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
     # username: root
     # password: 123456
     # driverClassName: com.mysql.jdbc.Driver
     # type: com.alibaba.druid.pool.DruidDataSource
     # check-process-definitions: false
spring:
  datasource:
    driver-class-name : com.mysql.cj.jdbc.Driver
    url : jdbc:mysql://localhost:3306/activiti?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
    username : root
    password : 123456

  jpa:
    show-sql : true

  activiti:
    ##校验流程文件，默认是校验resources下的processes文件夹里的流程文件
    ####在项目单独作为一个引擎，本身不部署流程的时候，如果resources目录没有“processes”目录，启动项目报错--找不到processes目录
    ##此处设置为false，即启动项目，不去校验processes
    check-process-definitions : false
